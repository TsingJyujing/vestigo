server:
  address: ":8080"
  database: "db.sqlite"
  # tokens: []  # Empty list - no authentication (default)
  # tokens:     # Example with authentication enabled
  #   - "your-secret-token-1"
  #   - "your-secret-token-2"
